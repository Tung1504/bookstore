@model bookstore.Models.Publisher
@{
    ViewBag.Title = "EditAuthor";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>
<div class="dashboard-wrapper">
    <div class="container-fluid  dashboard-content">
        @using (Html.BeginForm("Edit", "ManagePublisher", FormMethod.Post, new { @class = "form" }))
        {
            @Html.ValidationSummary();
            @Html.AntiForgeryToken();
            <div>
                <label>Publiser New Name</label>
                @Html.TextBoxFor(m => m.publisher_name, new { type = "text", @class = "form-control" })

            </div>



            <br />
            <button type="submit">Submit</button>
        }
    </div>
</div>